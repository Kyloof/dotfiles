kitty
kitty -v
echo $ZDOTDIR
ls
ls ~/.local/kitty.app/bin
echo $PATH
nvim .zshenv
echo 'export PATH="$HOME/.local/kitty.app/bin:$PATH"' >> ~/.zshenv\
source ~/.zshenv\

kitty -v
nvim .zshenv
echo $PATH
nano ~/.local/share/applications/kitty.desktop
kitty
nvim ~/.local/share/applications/kitty.desktop\

update-desktop-database ~/.local/share/applications\

nvim .zshenv
source .zshenv
echo $PATH
echo $PATH | grep -i kitty
echo $PATH | cut -d / | grep -i kitty
echo $PATH | cut -d / 1 | grep -i kitty
echo $PATH | cut -d /1 | grep -i kitty
echo $PATH | cut -d /  | grep -i kitty
cut --help
echo $PATH | cut -d : -f 1,2,3,4,5,6,7  | grep -i kitty
nvim $ZDOTDIR/completon.zsh
man autoload
autoload --help
autoload -h
echo $fpath
nvim $ZDOTDIR/completon.zsh
nvim $ZDOTDIR/.zshrc
ls
cd $ZDOTDIR
ls
mv completon.zsh completion.zsh
ls
source .zshrc
cat /proc/driver/nvidia/version
sudo ubuntu-drivers list
sudo ubuntu-drivers list
sudo ubuntu-drivers install nvidia:580
cat /proc/driver/nvidia/version
cat /proc/driver/nvidia/version
lsmod | grep nvidia
